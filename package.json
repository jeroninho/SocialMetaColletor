{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@10.17.1",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm --filter @workspace/api-server test && pnpm --filter @workspace/db test",
    "test:unit": "pnpm --filter @workspace/api-server run test:unit",
    "test:integration": "pnpm --filter @workspace/api-server run test:integration",
    "test:server-e2e": "pnpm --filter @workspace/api-server run test:e2e",
    "test:e2e": "pnpm --filter @workspace/playwright-e2e test",
    "test:browser": "pnpm --filter @workspace/playwright-e2e test"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
